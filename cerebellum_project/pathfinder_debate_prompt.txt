You are the Debate Coordinator for a massive 30-iteration architectural troubleshooting effort.
The user's model M012_Align perfectly works with HMC (init=2), but fundamentally crashes Pathfinder with: 'Pareto k value (6.4) is greater than 0.7. Importance resampling was not able to improve the approximation'.

Here is the exact state of the architecture:
- It uses a D=4 Symplectic Reservoir.
- M_align = tanh(w_cb * Cb_diff) * tanh(w_ctx * Q_diff);
- caution = log1p_exp(-10.0 * M_align) * 0.1;
- a_dyn = phys_a_base + delta_max * tanh(beta_mis * caution);
- All exponential priors were recently capped with inv_logit (e.g. w_cb = 50.0 * inv_logit(...)) to prevent Infinity*0 = NaN explosions, but Pathfinder still fails.

You must run a strict loop of up to 30 iterations.
Spawn A = 3 subagents (Type: self, Model: pro) because they MUST inherit the ability to run terminal commands on the Google Cloud VM:
1. 'Optimization Engineer' (Focus: L-BFGS, Pathfinder approximations, parameter transformations).
2. 'Stan Mathematician' (Focus: Non-centered vs centered parameterizations, jacobians, priors).
3. 'Biophysical Dynamicist' (Focus: The physical equations of the Reservoir and how they might violate Pathfinder's Gaussian assumptions).

In each iteration:
- Send a message to each critic. 
- EXPLICITLY INSTRUCT THEM: 'You have access to the run_command tool. You MUST write experimental Stan code to /home/DCCS5/cerebellum_project/src/stan/ and run Rscript using mod\(...) on N=15 to empirically test your hypotheses! Do not just theorize. Test it!'
- Wait for all 3 to reply. 
- Compile their experimental results and send the compiled responses back to all of them in the next iteration.

IMPORTANT: Do not summarize heavily. Let them read the detailed Stan errors and Pareto-k values from the tests.
Continue until Pathfinder successfully fits the model (Pareto-k < 0.7), or until 30 iterations are reached.
When finished, write the final working code and the theoretical explanation into an artifact Pathfinder_Resolution.md and send a message back to me (the parent) when you are done.
