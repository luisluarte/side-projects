\documentclass[11pt, a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage{amsmath, amssymb, amsthm}
\usepackage{geometry}
\geometry{margin=1in}
\usepackage{hyperref}
\usepackage{graphicx}
\usepackage{xcolor}

\title{\textbf{THE MAGI ROOT OBJECT:}\\ Hybrid cDDM with Autonomous Mutant Manifold}
\author{The MAGI Meta-Optimization Engine}
\date{\today}

\begin{document}
\maketitle

\begin{abstract}
This document formally defines the mathematical architecture of the Cortico-Cerebellar Evidence Accumulation system, derived via the MAGI protocol. By hybridizing a deterministic Drift-Diffusion Model (cDDM) with an autonomously fluctuating, Golgi-gated stochastic manifold, we formally resolve the topological paradox of high-frequency precision and macroscopic fatigue. The breakthrough of this formulation is the continuous convolutional generative evaluation, which analytically prevents the log-likelihood catastrophe caused by trial-by-trial physiological variance.
\end{abstract}

\section{I. The Cortical Value Basis (Deterministic Intent)}
The neocortex and striatum compute a continuous, deterministic low-dimensional value basis. This value generates the Reward Prediction Error ($\delta^{(k)}$) and drives the primary directional Drift Rate ($v^{(k)}$) of the cognitive decision process for trial $k$.
\begin{align}
\delta^{(k)} &= \text{Reward}^{(k)} - Q_{ctx}^{(k)} \\
v^{(k)} &= \kappa \cdot Q_{ctx}^{(k)}
\end{align}
\textbf{Decision Time Density:} The cognitive accumulation resolves over time $\tau$ according to the standard Wiener First Passage Time (WFPT). It is anchored with zero intrinsic non-decision time, as motor latency is handled separately:
\begin{equation}
f_{DT}(\tau) = f_{\text{WFPT}}(\tau \mid v^{(k)}, a, w)
\end{equation}

\section{II. The Cerebellar Variance Generator (Stochastic Expansion)}
The granular layer acts as an autonomous, uncoupled reservoir generating physiological variance.

\textbf{Intrinsic Drive:} $N$ uncoupled dimensions of continuous Poisson shot noise with basal rates $\mathbf{\lambda}$:
\begin{equation}
\mathbf{\zeta}^{(k)} \sim \text{PoissonShotNoise}(\mathbf{\lambda})
\end{equation}

\textbf{Sigmoidal Golgi Gating:} The instantaneous demand for computational complexity ($\mathcal{I}^{(k)}$) is defined by recent cortical volatility. This drives the global inhibitory conductance ($\theta^{(k)}$) via a non-linear sigmoid, protecting the system from overfitting pure noise during stable periods:
\begin{align}
\mathcal{I}^{(k)} &= \vert{}\delta^{(k-1)}\vert{} \\
\theta^{(k)} &= \frac{\theta_{max}}{1.0 + \exp\left(-\beta_g \cdot (\mathcal{I}^{(k)} - \mathcal{I}_{bias})\right)}
\end{align}

\textbf{The Uncoupled Mutant Manifold:} The stochastic drive is unmasked against the Golgi threshold and smeared across time via the log-uniform temporal leak matrix $\mathbf{\Lambda}$:
\begin{align}
\tilde{\mathbf{h}}^{(k)} &= \text{ReLU}\left( \mathbf{\zeta}^{(k)} - \theta^{(k)} \mathbf{1} \right) \\
\mathbf{h}^{(k)} &= (\mathbf{I} - \mathbf{\Lambda}) \mathbf{h}^{(k-1)} + \mathbf{\Lambda} \tilde{\mathbf{h}}^{(k)}
\end{align}

\section{III. The Unified Lasso Readout (Motor Automation)}
The unified Purkinje and Deep Cerebellar Nuclei (DCN) readout projects the active high-dimensional manifold ($V_{cb}^{(k)} = \mathbf{W}^{(k)} \mathbf{h}^{(k)}$) to dynamically govern the motor execution parameters.

\textbf{Mean Execution Latency ($\mu_{tnd}$):} Disinhibition of the DCN collapses the motor delay from a slow cortical default:
\begin{equation}
\mu_{tnd}^{(k)} = t_{nd\_max} - \gamma_{cb} \cdot \vert{}V_{cb}^{(k)}\vert{}
\end{equation}

\textbf{Execution Variance ($\sigma_{tnd}$):} The physiological jitter is strictly proportional to the total unmasked energy (the $\mathcal{L}_1$ norm) of the active granular manifold:
\begin{equation}
\sigma_{tnd}^{(k)} = \eta_{var} \cdot \Vert{}\mathbf{h}^{(k)}\Vert{}_1
\end{equation}

\textbf{Motor Time Density:} The physical execution time $t$ forms a normal probability distribution:
\begin{equation}
f_{MT}(t) = \mathcal{N}\left(t \mid \mu_{tnd}^{(k)}, \sigma_{tnd}^{(k)}\right)
\end{equation}

\section{IV. The Plasticity Operator (Heterosynaptic Subgradient Descent)}
The Purkinje readout weights update online upon receipt of the Climbing Fiber signal (the cortical RPE). The objective minimizes error while ruthlessly pruning unused connections via the $\mathcal{L}_1$ Lasso penalty ($\lambda_{lasso}$):
\begin{align}
\Delta \mathbf{W}^{(k)} &= \alpha \cdot \delta^{(k)} \cdot \left(\mathbf{h}^{(k)}\right)^\top - \eta_{decay} \cdot \lambda_{lasso} \cdot \text{sgn}\left(\mathbf{W}^{(k)}\right) \\
Q_{ctx}^{(k+1)} &= Q_{ctx}^{(k)} + \alpha_{ctx} \cdot \delta^{(k)}
\end{align}

\section{V. The Convoluted Generative Evaluation (The Topological Filter)}
To evaluate the true likelihood of the observed Reaction Time ($RT^{(k)}$) without triggering the $-\infty$ density cliff of trial-by-trial jitter, the model integrates the product of the deterministic decision time and the stochastic motor time via continuous convolution:
\begin{align}
P(RT^{(k)}) &= (f_{DT} * f_{MT})(RT^{(k)}) \\
P(RT^{(k)}) &= \int_{0}^{RT^{(k)}} f_{\text{WFPT}}(\tau \mid v^{(k)}, a, w) \cdot \mathcal{N}\left(RT^{(k)} - \tau \mid \mu_{tnd}^{(k)}, \sigma_{tnd}^{(k)}\right) \, d\tau
\end{align}

The global objective function for the Neural Architecture Search (NAS) is the minimization of the Negative Log-Likelihood over all $K$ trials:
\begin{equation}
\mathcal{L}_{MAGI} = - \sum_{k=1}^K \log P(RT^{(k)})
\end{equation}

\vspace{1cm}
\noindent\textbf{System Validation Summary:} \\
\textbf{Gate 0 (Rademacher):} Sustained by the Sigmoidal Golgi Gate ($\theta^{(k)}$). \\
\textbf{Gate 2 (Macroscopic Fatigue):} Sustained by the Uncoupled Leak Matrix ($\mathbf{\Lambda}$) driving the $\mu_{tnd}$ collapse. \\
\textbf{Gate 1 (Information Density):} Unlocked by the Convolutional Filter ($f_{DT} * f_{MT}$), absorbing the physiological manifold variance ($\sigma_{tnd}$) as valid probability mass.

\end{document}
