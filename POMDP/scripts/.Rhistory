emp_switch_rate ~ 1 + (1 | ID)
)
context_mdl <- lmer(
data = switch_params,
emp_switch_rate ~ droga + context + (1 | ID)
)
param_mdl <- lmer(
data = switch_params,
emp_switch_rate ~ context + droga +
kappa + phi + side +
beta + beta_slope + (1 | ID)
)
summary(param_mdl)
test_likelihoodratio(null_mdl, context_mdl, param_mdl)
anova(null_mdl, context_mdl, param_mdl)
param_mdl <- lmer(
data = switch_params,
emp_switch_rate ~ context + droga +
kappa + phi + (1 | ID)
)
summary(param_mdl)
test_likelihoodratio(null_mdl, context_mdl, param_mdl)
anova(null_mdl, context_mdl, param_mdl)
switch_params
switch_params <- posterior_medians_aligned %>%
mutate(
ID = as.factor(ID),
droga = as.factor(droga),
context = as.factor(context)
) %>%
left_join(emp_summary, by = c("ID", "droga", "context"))
null_mdl <- lmer(
data = switch_params,
emp_switch_rate ~ 1 + (1 | ID)
)
context_mdl <- lmer(
data = switch_params,
emp_switch_rate ~ droga + context + (1 | ID)
)
param_mdl <- lmer(
data = switch_params,
emp_switch_rate ~ context + droga +
kappa + phi + side +
beta + beta_slope + (1 | ID)
)
summary(param_mdl)
test_likelihoodratio(null_mdl, context_mdl, param_mdl)
anova(null_mdl, context_mdl, param_mdl)
param_mdl <- lmer(
data = switch_params,
emp_switch_rate ~ context + droga *
kappa + phi + side +
beta + beta_slope + (1 | ID)
)
summary(param_mdl)
test_likelihoodratio(null_mdl, context_mdl, param_mdl)
anova(null_mdl, context_mdl, param_mdl)
param_mdl <- lmer(
data = switch_params,
emp_switch_rate ~ context + droga +
kappa + phi + side +
beta + beta_slope + (1 | ID)
)
summary(param_mdl)
test_likelihoodratio(null_mdl, context_mdl, param_mdl)
anova(null_mdl, context_mdl, param_mdl)
param_mdl <- lmer(
data = switch_params,
emp_switch_rate ~ context + droga +
kappa * phi + side +
beta + beta_slope + (1 | ID)
)
summary(param_mdl)
test_likelihoodratio(null_mdl, context_mdl, param_mdl)
anova(null_mdl, context_mdl, param_mdl)
switch_params
param_mdl <- lmer(
data = switch_params,
log(emp_switch_rate) ~ context + droga +
kappa + phi + side +
beta + beta_slope + (1 | ID)
)
summary(param_mdl)
test_likelihoodratio(null_mdl, context_mdl, param_mdl)
anova(null_mdl, context_mdl, param_mdl)
null_mdl <- lmer(
data = switch_params,
log(emp_switch_rate) ~ 1 + (1 | ID)
)
context_mdl <- lmer(
data = switch_params,
log(emp_switch_rate) ~ droga + context + (1 | ID)
)
param_mdl <- lmer(
data = switch_params,
log(emp_switch_rate) ~ context + droga +
kappa + phi + side +
beta + beta_slope + (1 | ID)
)
summary(param_mdl)
test_likelihoodratio(null_mdl, context_mdl, param_mdl)
anova(null_mdl, context_mdl, param_mdl)
param_mdl <- lmer(
data = switch_params,
log(emp_switch_rate) ~ context + droga +
kappa * phi + side +
beta + beta_slope + (1 | ID)
)
summary(param_mdl)
test_likelihoodratio(null_mdl, context_mdl, param_mdl)
param_mdl <- lmer(
data = switch_params,
log(emp_switch_rate) ~ context + droga +
kappa + phi + side +
beta + (1 | ID)
)
summary(param_mdl)
test_likelihoodratio(null_mdl, context_mdl, param_mdl)
anova(null_mdl, context_mdl, param_mdl)
param_mdl <- lmer(
data = switch_params,
log(emp_switch_rate) ~ context + droga +
kappa + phi + side +
beta_slope + (1 | ID)
)
summary(param_mdl)
test_likelihoodratio(null_mdl, context_mdl, param_mdl)
anova(null_mdl, context_mdl, param_mdl)
param_mdl <- lmer(
data = switch_params,
log(emp_switch_rate) ~ context + droga +
kappa + phi +
beta + (1 | ID)
)
summary(param_mdl)
test_likelihoodratio(null_mdl, context_mdl, param_mdl)
anova(null_mdl, context_mdl, param_mdl)
param_mdl <- lmer(
data = switch_params,
log(emp_switch_rate) ~ context + droga +
kappa + phi + side +
beta + beta_slope + (1 | ID)
)
summary(param_mdl)
switch_params
switch_p
param_mdl <- glmmTMB(
data = switch_p,
switch_prob ~ context + droga +
kappa + phi + side +
beta + beta_slope + step + (1 | ID),
family = beta_family(link = "logit")
)
switch_params <- switch_p %>%
mutate(
ID = as.factor(ID),
droga = as.factor(droga),
context = as.factor(context)
) %>%
left_join(emp_summary, by = c("ID", "droga", "context"))
switch_params
switch_params <- posterior_medians_aligned %>%
mutate(
ID = as.factor(ID),
droga = as.factor(droga),
context = as.factor(context)
) %>%
left_join(switch_p, by = c("ID", "droga", "context"))
switch_params
switch_params <- switch_p %>%
mutate(
ID = as.factor(ID),
droga = as.factor(droga),
context = as.factor(context)
) %>%
left_join(posterior_medians_aligned, by = c("ID", "droga", "context"))
switch_params <- switch_p %>%
mutate(
ID = as.factor(ID),
droga = as.factor(droga),
context = as.factor(context)
) %>%
left_join(posterior_medians_aligned %>%
mutate(ID = as.factor(ID)), by = c("ID", "droga", "context"))
switch_params
switch_params <- switch_p %>%
mutate(
ID = as.factor(ID),
droga = as.factor(droga),
context = as.factor(context)
) %>%
left_join(posterior_medians %>%
mutate(ID = as.factor(ID)), by = c("ID", "droga", "context"))
posterior_medians
switch_params <- switch_p %>%
mutate(
ID = as.factor(ID),
droga = as.factor(droga),
context = as.factor(context)
) %>%
left_join(posterior_medians %>%
mutate(ID = as.factor(ID),
droga = as.factor(droga),
context = as.factor(context)), by = c("ID", "droga", "context"))
switch_params
posterior_median
posterior_medians
switch_params <- switch_p %>%
mutate(
ID = as.factor(ID),
droga = as.factor(droga),
context = as.factor(context)
) %>%
left_join(posterior_medians_aligned %>%
mutate(ID = as.factor(ID)), by = c("ID", "droga", "context"))
switch_params
posterio_medians_al
posterior_medians_aligned
switch_params <- switch_p %>%
mutate(
ID = as.factor(as.numeric(ID)),
droga = as.factor(droga),
context = as.factor(context)
) %>%
left_join(posterior_medians_aligned %>%
mutate(ID = as.factor(ID)), by = c("ID", "droga", "context"))
switch_params
param_mdl <- glmmTMB(
data = switch_params,
switch_prob ~ context + droga +
kappa + phi + side +
beta + beta_slope + step + (1 | ID),
family = beta_family(link = "logit")
)
param_mdl <- glmmTMB(
data = switch_params,
switch_prob ~ context + droga +
kappa + phi + side +
beta + beta_slope + step_norm + (1 | ID),
family = beta_family(link = "logit")
)
param_mdl <- lmer(
data = switch_params,
switch_prob ~ context + droga +
kappa + phi + side +
beta + beta_slope + step_norm + (1 | ID)
)
summary(param_mdl)
test_likelihoodratio(null_mdl, context_mdl, param_mdl)
anova(null_mdl, context_mdl, param_mdl)
context_mdl <- lmer(
data = switch_params,
log(emp_switch_rate) ~ droga + context + (1 | ID)
)
null_mdl <- lmer(
data = switch_params,
switch_prob ~ 1 + (1 | ID)
)
context_mdl <- lmer(
data = switch_params,
switch_prob ~ droga + context + (1 | ID)
)
param_mdl <- lmer(
data = switch_params,
switch_prob ~ context + droga +
kappa + phi + side +
beta + beta_slope + step_norm + (1 | ID)
)
summary(param_mdl)
test_likelihoodratio(null_mdl, context_mdl, param_mdl)
null_mdl <- lmer(
data = switch_params,
switch_prob ~ 1 + (1 | ID)
)
context_mdl <- lmer(
data = switch_params,
switch_prob ~ droga + context + (1 | ID)
)
param_mdl <- lmer(
data = switch_params,
switch_prob ~ context + droga +
kappa + phi + side +
beta + beta_slope + step_norm + (1 | ID)
)
summary(param_mdl)
test_likelihoodratio(null_mdl, context_mdl, param_mdl)
anova(null_mdl, context_mdl, param_mdl)
null_mdl <- lmer(
data = switch_params,
switch_prob ~ 1 + step_norm + (1 | ID)
)
context_mdl <- lmer(
data = switch_params,
switch_prob ~ droga + context + step_norm + (1 | ID)
)
param_mdl <- lmer(
data = switch_params,
switch_prob ~ context + droga +
kappa + phi + side +
beta + beta_slope + step_norm + (1 | ID)
)
summary(param_mdl)
test_likelihoodratio(null_mdl, context_mdl, param_mdl)
anova(null_mdl, context_mdl, param_mdl)
summary(param_mdl)
test_likelihoodratio(context_mdl, param_mdl)
test_likelihoodratio(null_mdl, context_mdl)
switch_param <- switch_params %>%
ungroup() %>%
drop_na()
null_mdl <- lmer(
data = switch_params,
switch_prob ~ 1 + step_norm + (1 | ID)
)
context_mdl <- lmer(
data = switch_params,
switch_prob ~ droga + context + step_norm + (1 | ID)
)
param_mdl <- lmer(
data = switch_params,
switch_prob ~ context + droga +
kappa + phi + side +
beta + beta_slope + step_norm + (1 | ID)
)
summary(param_mdl)
test_likelihoodratio(null_mdl, context_mdl, param_mdl)
context_mdl <- lmer(
data = switch_params,
switch_prob ~ droga + context + kappa + step_norm + (1 | ID)
)
param_mdl <- lmer(
data = switch_params,
switch_prob ~ context + droga +
kappa + phi + side +
beta + beta_slope + step_norm + (1 | ID)
)
summary(param_mdl)
test_likelihoodratio(context_mdl, param_mdl)
null_mdl <- lmer(
data = switch_params,
switch_prob ~ 1 + step_norm + side + (1 | ID)
)
context_mdl <- lmer(
data = switch_params,
switch_prob ~ droga + context + side + step_norm + (1 | ID)
)
param_mdl <- lmer(
data = switch_params,
switch_prob ~ context + droga +
kappa + phi + side +
beta + beta_slope + step_norm + (1 | ID)
)
summary(param_mdl)
test_likelihoodratio(context_mdl, param_mdl)
test_likelihoodratio(null_mdl, context_mdl, param_mdl)
anova(null_mdl, context_mdl, param_mdl)
emmeans(
param_mdl,
specs = ~ context + droga + kappa + phi + side + beta + beta_slope + step_norm,
type = "respose"
)
emmeans(
param_mdl,
specs = ~ context + droga + kappa + phi + side + beta + beta_slope + step_norm,
type = "respose",
at = list(kappa = seq(-15, 0, 1))
)
emmeans(
param_mdl,
specs = ~ context + droga + kappa + phi + side + beta + beta_slope + step_norm,
type = "respose",
at = list(kappa = seq(-15, 0, 1))
) %>%
broom::tidy(., conf.int = TRUE)
emmeans(
param_mdl,
specs = ~ context + droga + kappa + phi + side + beta + beta_slope + step_norm,
type = "respose",
at = list(kappa = seq(-15, 0, 1))
) %>%
broom::tidy(., conf.int = TRUE) %>%
ggplot(aes(
kappa, estimate
)) +
geom_line()
emmeans(
param_mdl,
specs = ~ context + droga + kappa + phi + side + beta + beta_slope + step_norm,
type = "respose",
at = list(kappa = seq(-15, 0, 1))
) %>%
broom::tidy(., conf.int = TRUE) %>%
ggplot(aes(
kappa, estimate, color = drug
)) +
geom_line()
test_likelihoodratio(null_mdl, context_mdl, param_mdl)
emmeans(
param_mdl,
specs = ~ context + droga + kappa + phi + side + beta + beta_slope + step_norm,
type = "respose",
at = list(kappa = seq(-15, 0, 1))
) %>%
broom::tidy(., conf.int = TRUE) %>%
ggplot(aes(
kappa, estimate, color = drug
)) +
geom_line()
emmeans(
param_mdl,
specs = ~ context + droga + kappa + phi + side + beta + beta_slope + step_norm,
type = "respose",
at = list(kappa = seq(-15, 0, 1))
) %>%
broom::tidy(., conf.int = TRUE) %>%
ggplot(aes(
kappa, estimate, color = droga
)) +
geom_line()
emmeans(
param_mdl,
specs = ~ context + droga + kappa + phi + side + beta + beta_slope + step_norm,
type = "respose",
at = list(kappa = seq(-15, 0, 1))
) %>%
broom::tidy(., conf.int = TRUE) %>%
ggplot(aes(
kappa, estimate, color = droga
)) +
geom_line() +
facet_wrap(~context)
test_likelihoodratio(null_mdl, context_mdl, param_mdl)
emmeans(
param_mdl,
specs = ~ context + droga + kappa + phi + side + beta + beta_slope + step_norm,
type = "respose",
at = list(phi = seq(-10, 0, 1))
) %>%
broom::tidy(., conf.int = TRUE) %>%
ggplot(aes(
kappa, estimate,
color = droga
)) +
geom_line() +
facet_wrap(~context)
emmeans(
param_mdl,
specs = ~ context + droga + kappa + phi + side + beta + beta_slope + step_norm,
type = "respose",
at = list(phi = seq(-10, 0, 1))
) %>%
broom::tidy(., conf.int = TRUE) %>%
ggplot(aes(
phi, estimate,
color = droga
)) +
geom_line() +
facet_wrap(~context)
emmeans(
param_mdl,
specs = ~ context + droga + kappa + phi + side + beta + beta_slope + step_norm,
type = "respose",
at = list(beta_slope = seq(0, 0.5, 0.1))
) %>%
broom::tidy(., conf.int = TRUE) %>%
ggplot(aes(
beta_slope, estimate,
color = droga
)) +
geom_line() +
facet_wrap(~context)
lmtest::lrtest(param_mdl)
emmeans(
param_mdl,
specs = ~ context + droga + kappa + phi + side + beta + beta_slope + step_norm,
type = "respose"
)
emmeans(
param_mdl,
specs = ~ context + droga + kappa + phi + side + beta + beta_slope + step_norm,
type = "respose"
) %>% test()
emmeans(
param_mdl,
specs = ~ context + droga + kappa + phi + side + beta + beta_slope + step_norm,
type = "respose"
) %>% pairs()
emmeans(
param_mdl,
specs = ~ kappa + phi + side + beta + beta_slope + step_norm | context | droga,
type = "respose"
) %>% pairs()
emmeans(
param_mdl,
specs = ~ kappa + phi + side + beta + beta_slope + step_norm + context | droga,
type = "respose"
) %>% pairs()
emmeans(
param_mdl,
specs = ~ context + droga + kappa + phi + side + beta + beta_slope + step_norm,
type = "respose",
at = list(kappa = seq(-15, 0, 1))
) %>%
broom::tidy(., conf.int = TRUE) %>%
ggplot(aes(
kappa, estimate,
color = droga
)) +
geom_line() +
facet_wrap(~context)
switch_params
switch_params$switch_prob %>% summary
